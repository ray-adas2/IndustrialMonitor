<UserControl x:Class="IndustrialMonitor.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="220">
    <UserControl.Resources>
        <!-- 菜单项样式 -->
        <Style x:Key="MenuItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="3,0,0,0"
                                BorderBrush="Transparent">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" FontSize="16" Margin="0,0,12,0"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Title}" VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1E293B"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1E3A5F"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter Property="Foreground" Value="#F1F5F9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#0F172A">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 平台 Logo -->
                <RowDefinition Height="*"/>     <!-- 菜单列表 -->
                <RowDefinition Height="Auto"/>  <!-- 底部版本信息 -->
            </Grid.RowDefinitions>

            <!-- 平台 Logo -->
            <StackPanel Grid.Row="0" Margin="20,24,20,20">
                <TextBlock Text="🏭" FontSize="28" HorizontalAlignment="Center"/>
                <TextBlock Text="工业监控平台" FontSize="16" FontWeight="Bold"
                           Foreground="#F1F5F9" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                <Line Stroke="#334155" X1="0" Y1="0" X2="1" Y2="0"
                      Stretch="Fill" Margin="0,12,0,0"/>
            </StackPanel>

            <!-- 菜单列表 -->
            <ListBox Grid.Row="1" x:Name="MenuListBox"
                     ItemsSource="{Binding MenuItems}"
                     SelectedItem="{Binding SelectedMenuItem, Mode=TwoWay}"
                     ItemContainerStyle="{StaticResource MenuItemStyle}"
                     Background="Transparent"
                     BorderThickness="0"
                     SelectionMode="Single"/>

            <!-- 底部信息 -->
            <Border Grid.Row="2" Padding="16,12" Margin="0,8,0,0">
                <StackPanel>
                    <Line Stroke="#334155" X1="0" Y1="0" X2="1" Y2="0"
                          Stretch="Fill" Margin="0,0,0,10"/>
                    <TextBlock Text="Industrial Monitor v1.0" FontSize="11"
                               Foreground="#475569" HorizontalAlignment="Center"/>
                    <TextBlock Text=".NET 8 + WPF" FontSize="10"
                               Foreground="#334155" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
